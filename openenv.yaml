[project]
name = "legal-contract-review"
version = "0.1.0"
description = "OpenEnv Legal Contract Review Environment"
authors = [{name = "Pratham Chawdhry"}]
requires-python = ">=3.9"

dependencies = [
    "openenv-core>=0.2.0"
]

[project.scripts]
server = "server.app:main"

[tool.setuptools]
packages = ["src"]